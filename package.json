{
  "name": "constellate",
  "version": "0.1.2",
  "private": true,
  "description": "A hand-drawn, scale-aware star arpeggiator for Max for Live.",
  "scripts": { "build": "node scripts/build.mjs", "package": "npm run build && node scripts/freeze.mjs", "test": "node --test tests/*.test.cjs", "check": "npm run package && npm test" }
}
